// Import the 'product' and 'banner' schemas
import product from "./product";
import banner from "./banner";

// Create an array of schema types including 'product' and 'banner'
export const schemaTypes = [product, banner];
